source "https://rubygems.org"

# GitHub Pages builds your site automatically with this gem — pinning it
# keeps your local preview identical to what GitHub publishes.
gem "github-pages", group: :jekyll_plugins

# Local preview only (not used by GitHub):
gem "webrick", "~> 1.8"

platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
